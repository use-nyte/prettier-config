name: ESLint

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  lint:
    name: Lint Code
    uses: use-nyte/.github/.github/workflows/lint-eslint.yml@main
    secrets: inherit
